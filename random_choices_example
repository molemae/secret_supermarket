# %%
import numpy as np 
import pandas as pd

# %% 
matrix = pd.read_csv('data/transition_matrix.csv')
m = np.array(matrix)
# %%
a = np.array([0,1,0,0,0,0])
# %%
a.dot(m)
# %%
sections = list(matrix.columns)

location= 'dairy'

# %%
np.random.choice(sections, 1, p=list(matrix.loc[location]))

# %%
